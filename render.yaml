services:
  - type: web
    name: ai-orchestration
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:create_app() --bind 0.0.0.0:$PORT
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DB_HOST
        value: host213.dungbhumi.com
      - key: DB_USER
        value: aiorchestration
      - key: DB_PASSWORD
        value: Aa!35741
      - key: DB_NAME
        value: aiorchestration
      - key: OPENAI_API_KEY
        sync: false
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: REDIS_URL
        fromService:
          type: redis
          name: ai-orchestration-cache
          property: connectionString
      - key: RATELIMIT_DEFAULT
        value: "100 per day"
      - key: RATELIMIT_STORAGE_URL
        fromService:
          type: redis
          name: ai-orchestration-cache
          property: connectionString
      - key: PASSWORD_SALT
        generateValue: true
    autoDeploy: true

  - type: redis
    name: ai-orchestration-cache
    ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere
    plan: free
